<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.misonamoo.niaportal.mapper.UserMapper">

    <!-- 로그인 -->
    <select id="login" parameterType="user" resultType="user">
		SELECT EMAIL as email,
				PASSWORD as password
		FROM
			AI_USER
		WHERE EMAIL = #{email}
		AND PASSWORD = #{password}
	</select>


    <!-- 비밀번호 재설정 -->
    <update id="setPw">
 	update AI_USER set
	   PASSWORD = #{password}
 	where EMAIL = #{email}
 	</update>

    <!-- 이메일 중복 체크 -->
    <select id="dupEmail" parameterType="user" resultType="int">
		select count(*)
		from AI_USER
		where EMAIL= #{email};
	</select>

    <insert id="regist" parameterType="user">
		INSERT INTO bizmiso.AI_USER (
			USER_NO,
			EMAIL,
			 PASSWORD,
			 USER_NAME,
			 TEL,
			 AGENCY,
			 COMPANY_TYPE_CODE,
			 DW_CONFIRM_YN,
			 REG_DATE,
			 UPD_DATE)
		VALUES (
			ai_get_seq('userNo'),
			#{email},
			#{password},
			#{userName},
			#{tel},
			#{agency},
			#{companyTypeCode},
			'N',
			NOW(),
			NOW()
		);
	</insert>

    <!-- 회원 탈퇴 -->
    <delete id="delete" parameterType="user">
		DELETE FROM bizmiso.AI_USER
		WHERE email = #{email};
	</delete>

    <select id="findUserNo" parameterType="user" resultType="int">
		 SELECT USER_NO FROM AI_USER WHERE EMAIL = #{email}
	</select>
</mapper>